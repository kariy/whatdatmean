"use strict";

// Copy this file to config.js and optionally paste your Gemini API key.
// config.js must exist for the extension to load (it is listed in the
// manifest's background scripts), but the key may be left empty — you can
// also set the key in the extension's settings page instead.
const WDM_DEFAULT_API_KEY = "";
